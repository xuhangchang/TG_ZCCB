
一、各个文件存放路径
	1. 执行程序路径：./bin
	2. 图片路径：./bmp
	3. 特征值数据路径：./dat
	4. 算法库路径：./lib
	5. 配置路径：./config
	6. 输出结果路径：./result    

二、执行
	1.特征提取:进入./bin目录，执行./extractFeature,会在特征值数据路径下生成对应的特征值数据，并在result目录下生成结果文件extract_result.txt
	2.比对：进入./bin目录，执行./match,在result目录下生成结果文件match_result.txt
	3.结果分析：进入./bin目录,执行./rate,在result目录下生成结果文件rate_result.txt

三、说明
	1.在配置文件中可以修改结果文件的路径（分别为特征提取、比对、结果分析）和原始图片目录路径、特征值存放目录。
	2.原始图片目录中的图片需按顺序存放，即1.bmp  2.bmp  ... n.bmp。并且1.bmp和2.bmp为同类，3.bmp和4.bmp为同类，以此类推。
	3.执行的时候按顺序执行，即先特征提取，再比对，最后结果分析
	4.执行match的时候，每6M写一次文件。
	5.此版本为稠州银行版本，原始图片的同类的两张图片在不同文件夹，即*_1.bmp都在A文件夹，*_2.bmp在B文件夹。
	6.提取特征生成的.dat还是在原来的文件夹。
	7.比对的时候A文件夹的静脉不跟自己文件夹的静脉比对，只和B文件夹的静脉比对。即100组图片，200张，比对次数为100*100=10000.


V1.2

2018/01/25 14:39
